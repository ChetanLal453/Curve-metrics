'use client'

import ComponentContainerCard from '@/components/ComponentContainerCard'

const ProfileDetail = () => {
  return (
    <ComponentContainerCard title="Profile">
      <div className="p-4 text-muted">
        This profile panel has been reduced to a safe placeholder while we stabilize the admin bundle.
      </div>
    </ComponentContainerCard>
  )
}

export default ProfileDetail
