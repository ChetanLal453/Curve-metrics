'use client'

import ComponentContainerCard from '@/components/ComponentContainerCard'

const AllAdvancedElements = () => {
  return (
    <ComponentContainerCard title="Advanced Form Elements">
      <div className="p-4 text-muted">
        Advanced form demo components are temporarily unavailable while the page editor build issues are being cleaned up.
      </div>
    </ComponentContainerCard>
  )
}

export default AllAdvancedElements
