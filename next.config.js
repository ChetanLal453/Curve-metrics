/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['www.digitalinfoways.com'], // add the external domains you use
  },
}

module.exports = nextConfig
